{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Association{% endblock %}

{% block body %}
    <h1>Association</h1>

    <table class="table">
        <tbody>
{#            <tr>
                <th>Id</th>
                <td>{{ association.id }}</td>
            </tr>#}
            <tr>
                <th>Name</th>
                <td>{{ association.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ association.description }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ association.category }}</td>
            </tr>
            <tr>
                <th>Creation_date</th>
                <td>{{ association.creationDate ? association.creationDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Address_number</th>
                <td>{{ association.addressNumber }}</td>
            </tr>
            <tr>
                <th>Address_street</th>
                <td>{{ association.addressStreet }}</td>
            </tr>
            <tr>
                <th>Address_town</th>
                <td>{{ association.addressTown }}</td>
            </tr>
            <tr>
                <th>Address_postcode</th>
                <td>{{ association.addressPostcode }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ association.website }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ association.telephond }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ association.mail }}</td>
            </tr>
            <tr>
                <th>IsRefuge</th>
                <td>{{ association.isRefuge ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('association_index') }}">back to list</a>

    <a href="{{ path('association_edit', {'id': association.id}) }}">edit</a>

    {{ include('association/_delete_form.html.twig') }}
{% endblock %}
