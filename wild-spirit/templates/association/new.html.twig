{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}New Association{% endblock %}

{% block body %}
    <section class="new">
        <div class="container">
            <h1>Create new Association</h1>

            {{ include('association/_form.html.twig') }}

            <a href="{{ path('association_index') }}">Retour à la liste</a>
            <a href="{{ path('index') }}">Retour au site</a>
        </div>
    </section>
{% endblock %}
